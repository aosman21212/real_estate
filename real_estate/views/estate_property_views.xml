<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Tree / List View -->
    <record id="estate_property_view_tree" model="ir.ui.view">
        <field name="name">real_estate.property.tree</field>
        <field name="model">real_estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties" create="1" delete="1">
                <field name="name"/>
                <field name="property_type_id"/>
                <field name="postcode"/>
                <field name="tag_ids" widget="many2many_tags"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="state" column_invisible="1"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">real_estate.property.form</field>
        <field name="model">real_estate.property</field>
        <field name="arch" type="xml">
            <form string="Property">
                <header>
                    <button string="Mark as Sold" name="action_sold" type="object"
                            class="oe_highlight" invisible="state != 'offer_accepted'"/>
                    <button string="Cancel" name="action_cancel" type="object"
                            invisible="state in ('sold', 'canceled')"/>
                </header>
                <sheet>
                    <field name="state" widget="statusbar"
                           statusbar_visible="new,offer_received,offer_accepted,sold"/>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="e.g. Cozy apartment in Brussels surroundings"/>
                        </h1>
                    </div>
                    <field name="tag_ids" widget="many2many_tags"/>
                    <group>
                        <group string="Property Details">
                            <field name="property_type_id"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group string="Financial">
                            <field name="expected_price"/>
                            <field name="best_price"/>
                            <field name="selling_price"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description" name="description">
                            <field name="description"/>
                            <group>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area"/>
                                <field name="total_area"/>
                            </group>
                        </page>
                        <page string="Offers" name="offers">
                            <field name="offer_ids">
                                <list string="Offers" create="1" delete="1">
                                    <field name="price"/>
                                    <field name="partner_id"/>
                                    <field name="validity"/>
                                    <field name="deadline"/>
                                    <field name="status" column_invisible="1"/>
                                    <button name="action_accept" type="object" icon="fa-check" string="Accept"/>
                                    <button name="action_refuse" type="object" icon="fa-times" string="Refuse"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">real_estate.property.search</field>
        <field name="model">real_estate.property</field>
        <field name="arch" type="xml">
            <search string="Properties">
                <field name="name"/>
                <field name="postcode"/>
                <filter string="Available" name="available" domain="[('state', '=', 'new')]"/>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">real_estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="estate_property_view_search"/>
    </record>

</odoo>
